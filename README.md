# `pnpm approve-builds` may be needed

ast-grep(sg) is a CLI tool for code structural search, lint, and rewriting.

## Quick start

```bash
npm install --global @ast-grep/cli
# `pnpm approve-builds` may be needed
pip install ast-grep-cli
brew install ast-grep
```

```bash
cargo install ast-grep --locked
cargo binstall ast-grep
# install via scoop, thank @brian6932
scoop install main/ast-grep
# install via MacPorts
sudo port install ast-grep
# try ast-grep in nix-shell
nix-shell -p ast-grep
# try ast-grep with mise
mise use -g ast-grep
```

## Docs

- [abstract syntax tree](https://dev.to/balapriya/abstract-syntax-tree-ast-explained-in-plain-english-1h38)
- [`grep`](https://en.wikipedia.org/wiki/Grep#:~:text=grep%20is%20a%20command%2Dline,which%20has%20the%20same%20effect.)
- [wildcard](https://en.wikipedia.org/wiki/Wildcard_character)
- [regular expression dot](https://regexone.com/lesson/wildcards_dot)
- [online playground](https://ast-grep.github.io/playground.html)
